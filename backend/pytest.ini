[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Force INFO so in-handler logger.info() calls actually build records during
# tests — catches reserved-LogRecord-attribute / extra-key bugs that only fire
# under the live uvicorn config (root at INFO) otherwise.
log_level = INFO
