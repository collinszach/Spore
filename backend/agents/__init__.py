"""Runtime Claude API agents (Epic 3+).

These run Spore's triage/cognition pipeline against real captures, as
distinct from the build-time `.claude/agents/` subagents that write Spore's
own code. See CLAUDE.md "Agent layers (do not conflate)".
"""
