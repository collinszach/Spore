# Spore backend — Story 1.1 (stand up the stack)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# Story 1.2 — data layer
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pgvector==0.3.6

# Epic 3 — Sorter + embeddings + dedup (runtime agents)
anthropic==0.69.0

# Epic 5 — Obsidian vault integration (pure-python git, no system git binary)
dulwich==0.22.8

# Epic 6 — Skills engine (declarative *.skill.yaml loader)
pyyaml==6.0.2

# Test deps
pytest==8.3.3
httpx==0.27.2
pytest-asyncio==0.24.0
anyio==4.6.0
